  def callback(self):
       an=input(" what was the war.. was it a, b, or c")
       if (an =="a"):
           print("good job")
       else:
            print("try again")
            
            
            make methods and then within buttons call them using command = (method)
